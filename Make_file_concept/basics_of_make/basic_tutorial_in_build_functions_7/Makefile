# Make provides several built-in functions, such as:

# $(wildcard *.c) → Finds all .c files in the current directory.
# $(patsubst pattern,replacement,text) → Modifies file extensions or replaces parts of strings.
# $(shell command) → Runs shell commands within the Makefile.
# $(sort text) → Sorts a list of words.
# $(filter pattern,text) → Filters words that match a pattern.
# $(if condition,true,false) → Conditional execution.
# $(foreach var,list,text) → Loops over a list.